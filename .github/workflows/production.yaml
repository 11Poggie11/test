name: Main to Production
on:
  push:
    branches:
      - main
jobs:
  copy-to-production:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout main branch
      uses: actions/checkout@v2
      with:
        ref: main
    - name: Copy files to production branch
      run: |
        git config --global user.email "492992@student.fontys.nl"
        git config --global user.name "11poggie11"
        rm -r test
        git clone https://11poggie11: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@github.com/11Poggie11/test.git
        cd test
        git checkout production
        git merge main
        git push -u origin production
