name: Main to Production

on:
  push:
    branches:
      - main

jobs:
  copy_to_production:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Copy main to production
        run: |
          git branch -f production main
          git push origin production --force
